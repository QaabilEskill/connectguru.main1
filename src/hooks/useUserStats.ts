// Stats hook kept as a stub after course/lesson features were removed.
export const useUserStats = () => ({
  lessonsCompleted: 0,
  storiesRead: 0,
  totalActivities: 0,
  refreshStats: async () => {},
});
